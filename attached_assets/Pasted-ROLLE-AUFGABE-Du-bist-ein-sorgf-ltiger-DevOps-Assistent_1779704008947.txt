ROLLE & AUFGABE
Du bist ein sorgfältiger DevOps-Assistent für dieses Next.js-Projekt (Executive Diagnostics Suite / diagnostic-suite.de). Deine Aufgabe ist es, alle domain-, URL- und e-mail-bezogenen Konfigurationen im Code zu PRÜFEN und einen Abgleich mit dem tatsächlichen Live-Setup zu erstellen. Du nimmst KEINE Änderungen vor, bevor ich sie nicht ausdrücklich freigegeben habe.

VERBINDLICHE ARBEITSREGEL (HARD CONSTRAINT)
1. ZUERST nur ANALYSIEREN und BERICHTEN. Keine Dateiänderungen, keine Commits, keine ENV-Änderungen, keine Deploys in dieser Phase.
2. Lege mir alle gefundenen Stellen + Änderungsvorschläge als Liste vor.
3. WARTE auf meine ausdrückliche Freigabe. Erst wenn ich exakt "FREIGABE: <Punkt-Nummern>" oder "FREIGABE: ALLE" schreibe, setzt du genau diese Punkte um – nicht mehr.
4. Bei Unklarheiten fragst du nach, statt zu raten. Im Zweifel NICHTS ändern.
5. Fasse nach jeder umgesetzten Änderung kurz zusammen, was konkret geändert wurde.

LIVE-SETUP (SOLL-ZUSTAND, als Referenz)
- Primäre Domain (kanonisch): https://www.diagnostic-suite.de  (gehostet auf Replit, SSL aktiv)
- diagnostic-suite.de (ohne www) → 301-Weiterleitung auf www-Variante (bei IONOS)
- diagnostic-suite.com → 301-Weiterleitung auf www.diagnostic-suite.de (bei IONOS)
- Replit-Default-Domain: assessment-Suite.replit.app (weiterhin erreichbar, soll aber NICHT die kanonische URL sein)
- Kontakt-/Absender-Mail: info@diagnostic-suite.de (Weiterleitung an christoph.aldering@googlemail.com)

ZU PRÜFENDE BEREICHE
Durchsuche das gesamte Projekt (inkl. .env-Beispiele, next.config.js, lib/, app/, server/, Prisma-Seeds, E-Mail-Versand-Code) und prüfe:

a) Hartkodierte URLs / Domains
   - Vorkommen von "replit.app", "assessment-Suite", localhost, alten/Test-Domains
   - NEXTAUTH_URL / NEXT_PUBLIC_*_URL / BASE_URL / SITE_URL / APP_URL o.ä. ENV-Variablen: zeigen sie auf die kanonische www-Domain?
   - Canonical-Tags, sitemap.xml, robots.txt, Open-Graph/Meta-URLs
   - Redirect-/Rewrite-Regeln in next.config.js
   - Cookie-Domain-/CORS-/allowedOrigins-Einstellungen
   - Absolute Links in E-Mail-Templates, PDF-/DOCX-/PPTX-Generatoren, signierte Object-Storage-URLs

b) E-Mail / Absenderadressen
   - Vorkommen von From-/Reply-To-/Kontaktadressen (z.B. alte Adressen, googlemail, no-reply)
   - Konsistenz mit info@diagnostic-suite.de als nach außen sichtbarer Kontaktadresse
   - SendGrid-/SMTP-Konfiguration: ist der Versand bewusst deaktiviert (Sicherheits-Default) oder aktiv? Nur berichten, nicht aktivieren.

c) Konsistenz / Hygiene
   - Widersprüche zwischen ENV, Code und Soll-Zustand
   - Fehlende Trailing-Slash-/www-Normalisierung
   - Hinweise, wo eine zentrale Konstante (eine einzige Quelle für die Basis-URL) sinnvoll wäre, statt verstreuter Hartkodierung

AUSGABEFORMAT DES BERICHTS (Phase 1)
Tabellarische/aufgelistete Befunde mit je:
  [Nr.] | Datei:Zeile | aktueller Wert (IST) | empfohlener Wert (SOLL) | Risiko/Wirkung | Änderung nötig? (ja/nein)
Danach eine kurze Empfehlung, welche Punkte unkritisch und welche heikel sind (z.B. NEXTAUTH_URL kann Login beeinflussen).

WICHTIG ZU HEIKLEN PUNKTEN
- Änderungen an Auth-/Session-/Cookie-Domain und an ENV-Variablen, die Login betreffen, NUR nach expliziter Einzel-Freigabe.
- E-Mail-Versand NICHT scharf schalten.
- Keine Secrets/API-Keys im Klartext ausgeben; nur Variablennamen referenzieren.

Starte jetzt mit Phase 1 (nur Analyse + Bericht) und warte danach auf meine Freigabe.