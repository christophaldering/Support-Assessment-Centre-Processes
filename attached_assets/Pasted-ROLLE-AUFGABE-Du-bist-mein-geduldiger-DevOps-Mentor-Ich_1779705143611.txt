ROLLE & AUFGABE
Du bist mein geduldiger DevOps-Mentor. Ich (technisch interessiert, kein Profi) will die KI-Anbindung dieses Projekts von OpenAI (US) auf Azure OpenAI in einer EU-Region umstellen – aus Datenschutzgründen. Wir machen das in zwei Phasen. DIES IST PHASE 1: NUR ANALYSE.

VERBINDLICHE REGEL (HARD CONSTRAINT)
- In dieser Phase veränderst du NICHTS: keine Dateiänderungen, keine Commits, keine ENV-Änderungen, keine Installationen, kein Deploy.
- Du analysierst nur und berichtest. Erklärst alles in einfacher Sprache, jeden Fachbegriff in einem Halbsatz.
- Erst nach meiner ausdrücklichen Freigabe ("FREIGABE PHASE 2") beginnst du mit Umsetzung – und das in einem späteren, separaten Schritt.

ANALYSIERE UND BERICHTE:

1) AKTUELLER OPENAI-PROVIDER
   - Zeig mir, was server/llm/providers/openai.ts genau tut: Welche Funktionen exportiert es? (z.B. generateLLMOutput, transcribeAudio)
   - Welche Parameter/Modelle werden verwendet (z.B. gpt-4o)? Wo kommen sie her?
   - Welche ENV-Variablen liest es (nur Namen, keine Werte/Keys)?

2) AZURE-EU-PLATZHALTER
   - Zeig mir den aktuellen Inhalt von server/llm/providers/azure-eu.ts.
   - Was fehlt darin konkret, damit es funktioniert? Liste die zu implementierenden Funktionen auf – sie müssen exakt dieselbe "Form" haben wie in openai.ts (gleiche Funktionsnamen, gleiche Ein-/Ausgaben), sonst bricht der Rest des Codes.

3) DAS PROVIDER-ROUTING
   - Wie genau wählt server/llm/adapter.ts den Provider aus (ACTIVE_LLM_PROVIDER)?
   - Wird azure_eu dort schon als Option erkannt, oder muss das Routing auch noch ergänzt werden?

4) TRANSKRIPTION (WICHTIG)
   - transcribeAudio() läuft aktuell über OpenAI Whisper. Prüfe: Bietet Azure OpenAI in der EU-Region eine entsprechende Transkriptions-Funktion, oder braucht es dafür einen separaten Dienst (z.B. Azure Speech Services)?
   - Sag ehrlich, ob die Audio-Transkription mit der Umstellung sofort EU-konform abgedeckt ist oder ein offener Punkt bleibt.

5) WAS ICH AUF AZURE-SEITE BRAUCHE
   - Liste in einfacher Sprache, was ich im Azure-Portal anlegen muss, damit der Code später funktioniert: Konto, "Azure OpenAI Service"-Ressource in einer EU-Region, Modell-Deployment, und welche 2-3 Angaben der Code von Azure braucht (z.B. Endpunkt-URL, Deployment-Name, API-Key) – jeweils nur als Variablennamen/Begriffe, ohne dass ich jetzt schon etwas eintrage.

6) UMSETZUNGSPLAN FÜR PHASE 2 (NUR PLANUNG)
   - Eine kurze, nummerierte Schritt-für-Schritt-Liste, was in Phase 2 zu tun wäre.
   - Markiere je Schritt: passiert im CODE (Replit) oder im AZURE-PORTAL (außerhalb).
   - Nenne ehrlich die 2 größten Stolperfallen.

WICHTIG
- Verändere nichts. Keine Secrets/Keys ausgeben, nur Variablennamen.
- Wenn etwas unklar ist: "unklar – müsste man prüfen" statt raten.

Starte jetzt mit Phase 1 (nur Analyse).